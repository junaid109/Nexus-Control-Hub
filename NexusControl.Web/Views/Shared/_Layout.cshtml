<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - NexusControl Hub</title>
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Styles -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
    <div class="app-container">
        <!-- Sidebar -->
        <aside class="sidebar glass-panel" style="margin: 1rem; height: calc(100vh - 2rem); position: fixed;">
            <div class="nav-brand">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/>
                </svg>
                NexusControl
            </div>
            
            <nav style="display: flex; flex-direction: column; gap: 0.5rem; margin-top: 2rem;">
                <a class="nav-item active" asp-controller="Home" asp-action="Index">
                    <span>📊</span> Dashboard
                </a>
                <a class="nav-item" asp-controller="Players" asp-action="Index">
                    <span>👤</span> Players
                </a>
                <a class="nav-item" asp-controller="Social" asp-action="Index">
                    <span>💬</span> Social Graph
                </a>
                <a class="nav-item" asp-controller="Assets" asp-action="Index">
                    <span>📦</span> Assets
                </a>
                <a class="nav-item" asp-controller="Infrastructure" asp-action="Index">
                    <span>📡</span> Servers
                </a>
                <a class="nav-item" asp-controller="Config" asp-action="Index">
                    <span>⚙️</span> Config
                </a>
            </nav>
            
            <div style="margin-top: auto;">
                <div class="nav-item">
                    <span>🔒</span> Admin
                </div>
            </div>
        </aside>

        <!-- Main Content -->
        <div class="main-content" style="margin-left: 280px;">
            <header class="glass-header" style="border-radius: 12px; padding: 1rem 2rem; display: flex; align-items: center; justify-content: space-between; margin-bottom: 2rem;">
                <div>
                    <h4 style="margin: 0; font-weight: 600;">@ViewData["Title"]</h4>
                    <span style="font-size: 0.85rem; color: var(--text-muted);">Welcome back, Admin</span>
                </div>
                
                <div style="display: flex; gap: 1rem; align-items: center;">
                    <button class="btn btn-secondary" style="padding: 0.5rem;">🔔</button>
                    <div style="width: 40px; height: 40px; border-radius: 50%; background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));"></div>
                </div>
            </header>

            <main role="main">
                @RenderBody()
            </main>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/8.0.0/signalr.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>

